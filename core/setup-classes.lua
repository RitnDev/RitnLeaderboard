-- setup classes
----------------------------------------------------------------------
require(ritnlib.defines.core.setup)
log('> imported : ' .. ritnlib.defines.core.setup)
require(ritnlib.defines.log.setup)
log('> imported : ' .. ritnlib.defines.log.setup)
----------------------------------------------------------------------
require(ritnlib.defines.leaderboard.class.force)
require(ritnlib.defines.leaderboard.class.synth)